# Dependencias extra para el bot de Telegram (instalar además de requirements.txt)
python-telegram-bot>=22.0
plotly>=5.18.0       # Para generar los gráficos que kaleido exporta a PNG
kaleido>=1.0          # Exporta las figuras de Plotly a PNG (/distribucion, /mismatches_chart)
# kaleido>=1.0 arranca su propio Chrome vía choreographer, no usa el del sistema.
# Tras instalar, correr una vez: choreo_get_chrome
